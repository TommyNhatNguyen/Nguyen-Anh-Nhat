export type PriceModel = {
  currency: string;
  date: string;
  price: number;
};


export type SelectedCurrencyType = {
  value: number;
  label: string;
  date: string;
};

export type ConversionResultType = {
  from: number;
  to: number;
};

